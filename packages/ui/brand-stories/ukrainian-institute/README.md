# Ukrainian Institute — archived Storybook stories

These stories are kept for the `ukrainian-institute` client (`apps/ui-home`) but are
**not** included in the default Storybook sidebar (Sternenko-focused kit).

To preview them, temporarily add this path to `.storybook/main.ts`:

```ts
"../brand-stories/ukrainian-institute/**/*.@(mdx|stories.tsx)",
```
