{
  "name": "sternenko",
  "version": "0.0.0",
  "private": true,
  "description": "БФ «Спільнота Стерненка» — сторінка звітів",
  "scripts": {
    "dev": "node scripts/dev-preview.mjs",
    "preview": "node scripts/dev-preview.mjs",
    "build": "VITE_BASE_PATH=/sternenko/ pnpm --dir source --filter sternenko-fund run build && rm -rf dist && cp -R source/apps/sternenko-fund/dist ./dist",
    "sync:storybook": "pnpm --dir source --filter spilnota-sternenka build && rm -rf dist && cp -R source/apps/spilnota-sternenka/dist ./dist"
  },
  "devDependencies": {
    "vite": "^8"
  }
}
